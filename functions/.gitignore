# Per-function demo_data copies are GENERATED by scripts/sync_shared.js from
# the canonical functions/demo_data/. They are a required predeploy step (see
# DEPLOY.md), not source — don't double-commit them.
packages/rollaway/*/demo_data/
